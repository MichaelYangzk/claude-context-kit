{
  "canary_pattern": "^///",
  "failure_threshold": 2,
  "auto_action": "warn",
  "check_interval": 2,
  "notification_style": "emoji",
  "_comments": {
    "canary_pattern": "Regex to check against Claude's output. Default: starts with ///",
    "failure_threshold": "Consecutive failures before critical alert",
    "auto_action": "Hook behavior: warn = inject warning | block = block message",
    "check_interval": "Daemon check interval in seconds",
    "notification_style": "Daemon notification: minimal | emoji | ascii | custom"
  }
}
